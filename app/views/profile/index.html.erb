<!DOCTYPE html>
<html>
<head>
  <title>Auto Scroll</title>
  <script>
    // Auto scroll function
    function autoScroll() {
      window.scrollBy(0, 100); // Scroll down by 1 pixel
    }

    // Start auto-scrolling once the page is fully loaded
    window.onload = function() {
      setInterval(autoScroll, 10); // Scroll every 10 milliseconds (adjust as needed)
    }
  </script>
</head>
<body>
  <div style="height: 2000px;"> <!-- Content that is taller than the window height -->
    <h1>Welcome to the Auto Scroll Page!</h1>
    <p>This page will scroll automatically.</p>
  </div>
</body>
</html>
