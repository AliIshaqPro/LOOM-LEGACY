<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ali Ishaq 3D Sphere</title>
  <%= javascript_include_tag 'index' %>

  <style>
    /* Basic styling for the content and container */
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: Arial, sans-serif;
      background-color: #000; /* Black background */
      overflow-y: auto;
    }

    .parallax {
      display: flex;
      justify-content: center;
      align-items: center;
      width:100%;
      z-index: 2; /* Ensure this is on top of the 3D scene */
      position: relative; /* To position the content on top */
    }

    .content {
      background-color:transparent;
      
      width: 100%;
      border-radius: 10px;
      text-align: center;
      justify-content:center;
      align-items:center;
      z-index: 3; /* Ensure content is above the sphere */
      position: relative; /* To ensure proper stacking */
      display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 100%;
    }

    h1 {
      font-size: 36px;
      color: #333;
      margin-bottom: 30px;
    }

    .card {
      background-color: #fff;
      padding: 20px;
      margin-top: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
    }


    #threejs-container {
      position: fixed; /* Ensures it stays in place */
      background-color: black;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      z-index: 1; 
    }
  </style>
</head>
<body>
<!-- Container for the Three.js scene -->
<div id="threejs-container" style="position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background-color: black; z-index: 1;"></div>

<!-- Content on top of the 3D sphere -->
<div class="parallax" style="display: flex; justify-content: center; align-items: center; width: 100%; height: 100vh; position: absolute; top: 0; left: 0; z-index: 2;">
  <div class="content" style="text-align: center; color: white;">
    <h1 style="font-size: 3rem; color:white;">ChatSphere</h1>
    <p class="fs-2" style="font-size: 50px;">Welcome to Chatsphere</p><br>
    <p class="fs-3">Thank You For Choosing Chatsphere</p><br>
    <a href="/whatsapp" style="font-size: 20px; border: 5px solid black; background-color: rgba(26, 22, 22, 0.3); backdrop-filter: blur(10px); border-radius: 50px; padding: 1rem; text-decoration: none; color: white;">Click Here To Proceed</a>
  </div>
</div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
  
</body>
</html>
