<%= form_with(model: loom_legacy) do |form| %>
  <% if loom_legacy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(loom_legacy.errors.count, "error") %> prohibited this loom_legacy from being saved:</h2>

      <ul>
        <% loom_legacy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
